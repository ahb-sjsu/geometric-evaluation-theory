# experiments

One directory per gate, created when its registration is sealed (PROTOCOL.md rule 1). None sealed yet.

`G2/` exists before its seal as an exception recorded here: it holds the checker, its self-test record, the draft registration, and the config templates, and no data. It is sealed by the procedure in `G2/PREREG-G2-DRAFT.md` Section 9.

`G4/` likewise exists before its seal: theory module with self-test record, probe and run scripts, workload fetcher, config with CONFIRM fields, draft registration. No probe output yet.
