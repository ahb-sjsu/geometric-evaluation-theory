{
  "world": "Llama-3.2-3B grouped-query attention, the cells, consumers, workload, operating points and recovered read operators of G4 (experiments/G4/probe_*.npy at commit dc14015); the perturbation is one key at one operating point by eps (I - Q) u, u standard normal in whitened coordinates, antithetic pair, common random numbers",
  "model_id": "unsloth/Llama-3.2-3B",
  "revision": "d4446454d87d51aa42e1fb174f25acc5f8762331",
  "n_directions": 160,
  "layers": [
    8,
    16
  ],
  "n_kv_heads": 8,
  "n_q_heads": 24,
  "head_dim": 128,
  "workload_file": "../G4/workload.txt",
  "workload_source": "Project Gutenberg, Pride and Prejudice (public domain), fetched by fetch_workload.py, SHA-256 recorded at sealing",
  "n_tokens": 1024,
  "n_query_positions": 64,
  "n_operating_points": 64,
  "probe_step": 0.01,
  "weights": "equal",
  "seed": 20260907,
  "cpu_threads": 16,
  "probe_prefix": "probe",
  "versions_on_atlas_2026-09-07": {
    "python_env": "/archive/kvbench/venv",
    "torch": "2.8.0+cu128",
    "transformers": "4.56.1",
    "readscope": "0.1.0 (source tree /home/claude/readscope on PYTHONPATH; jacobian_probe(consumer, points, *, n_directions, eps, rng, batched))",
    "numpy": "2.5.1"
  },
  "workload_sha256_at_fetch": "80fcc29381c33fc73dcc07d237f05b860725fb7e50de8efaba155bcc10027d9e",
  "probe_dir": "../G4",
  "ranks": [
    2,
    4,
    8
  ],
  "n_random_codes": 16,
  "eps_ladder": [
    0.02,
    0.05,
    0.1,
    0.2
  ],
  "measured_loss": "summed squared change of the square-root attention weights over the sampled query positions, averaged over the antithetic pair and over the 64 operating points, divided by eps^2",
  "predicted_loss": "tr(Pt_i (I - Q)) with Pt_i the G4 probe operator (readscope pseudo-inverse recovery, exact at 160 directions in 128 dimensions, Gram summed over the vector output and averaged over the operating points)",
  "bars": {
    "P1_relative_error_max": 0.25,
    "P1_min_fraction": 0.8,
    "P1_at_every_rank": true,
    "P2_own_excess_over_measured_best_max": 0.05,
    "P2_min_cells": 12,
    "P3_compromise_tolerance": 0.25,
    "P3_min_fraction_of_nonvacuous_cells": 0.8,
    "P4_beats_bound_tolerance": 0.25,
    "P5_min_fraction_of_nonvacuous_cells": 0.8,
    "nonvacuous_cell_min_deficiency_fraction": 0.05,
    "anti_vacuity_rank": 2,
    "anti_vacuity_min_deficiency_fraction": 0.05,
    "anti_vacuity_min_cells": 12,
    "gate_rule": "per-eps per-rank verdicts by the G4 rule; gate verdict at the smallest eps at which P1 holds at every rank; INDETERMINATE with the ratio diagnosis if none"
  },
  "expected_calls": "16 cells x 64 points x 3 ranks x 21 codes x 4 eps x 2 = 1,548,288 group-consumer calls",
  "path_correction_2026-09-08": "workload_file pointed at the G4 directory by a bare name; corrected to ../G4/workload.txt after the first launch failed to open it before any measurement. Same file, same hash."
}