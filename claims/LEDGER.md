# GET claim ledger

One row per headline claim. Classes follow the observation-theory ledger: `[proved]`, `[demonstrated]`, `[replicated]`, `[predicted]`, `[exploratory]`, `[refuted]`, `[posited]`, `[open]`. A class is raised only by a sealed pass and lowered by a sealed miss. Every row names where the claim is stated and, once one exists, the gate and result that grade it. Machine-check names are filled at G1. Lean files live in `lean/GET/`, built on Atlas against Mathlib v4.32.2 with no `sorry`; the axiom audit is `lean/Axioms.lean`, and at the clean pass of 2026-09-07 every audited theorem depends only on `propext`, `Classical.choice`, and `Quot.sound`.

| ID | Claim | Class | Stated | Gate | Lean |
|---|---|---|---|---|---|
| GET-1 | Unbudgeted distinctions are an equivalence; a rank budget coarsens it; a length budget is a tolerance, transitive iff no three distances form an epsilon-chain that exceeds epsilon | `[proved]` | paper Thm 1 | G1 | `GET.Tolerance.not_trans_of_chain`, `GET.Tolerance.indiff_trans_of_chain`, `GET.Tolerance.indiff_zero_trans` (length-budget part) |
| GET-2 | Induced preference is a weak order with utility minus distance at zero threshold, a semiorder with threshold equal to the budget otherwise | `[proved]` | paper Thm 2 | G1 | `GET.Semiorder.pref_irrefl`, `pref_trans`, `pref_axiom1`, `pref_axiom2`, `trichotomy`, `pref_zero_iff` |
| GET-3 | Choice is nonempty on finite admissible menus; satisficing is the length budget | `[proved]` | paper Prop 1 | none | none |
| GET-4 | Representability for a fixed world is semidefinite feasibility; the hull law is necessary for every convex evaluation; affinely independent consequences represent every order; a line represents exactly the single-peaked orders | `[proved]` | paper Thm 3 | G1 (hull law), G2 (hull law measured) | `GET.HullLaw.hull_law`, `GET.HullLaw.hull_law_pref` (hull law); the other parts not yet checked |
| GET-5 | Metric identified up to scale and ideal up to the metric's kernel from the order on an open set | `[proved]` (theorem) / `[demonstrated]` in the synthetic stage (G5, sealed, PASS 12 of 12 cells: from a weak order on 64 points above the dimension the metric and the ideal's range component are recovered to a few percent of chance, error falling monotonically with the battery; nothing recovered at or below the design bound, off the span of a subspace battery, or in a singular metric's kernel; semiorders slower, 5 to 25 percent of chance) | paper Thm 4 | G1, G5 | `GET.Uniqueness.ideal_unique`, `GET.Uniqueness.scale_free` (one dimension); `GET.UniquenessGeneral.uniqueness` with `ideal_in_kernel`, `qf_proportional`, `polarization`, `eq_of_qf_eq` (general dimension, same order on all of the space) |
| GET-6 | EU, mean-variance, additive MAUT and ideal-point models, TCSS decision cost, satisficing, Dawid-Lauritzen geometry, Nash are special cases with stated conditions | `[proved]` | paper Prop 2 | none | none |
| GET-7 | Rank-budget change reverses preference between fixed actions | `[proved]` (existence) / `[predicted]` (in evaluators) | paper Thm 5, prediction (ii) | G3, G7 | `GET.Reversal.rank_one_prefers_b`, `rank_two_prefers_a`, `no_common_utility` |
| GET-8 | Same standard iff ordinally equivalent induced distances; agreement on one menu does not transfer | `[proved]` | paper Thm 6 | none | none |
| GET-9 | Neutral shared rank-k representation iff common top-k eigenspace; least weighted regret at top-k eigenspace of the weighted sum, positive otherwise | `[proved]` (theorem) / `[exploratory]` on shared KV codes (G4, sealed, INDETERMINATE at ranks 2, 4, 8: measured attention loss 20 to 330 times the quadratic prediction, so the regret formula and bound were not tested; the ordering claim on measured losses held in 32 of 32 non-vacuous cell-ranks; the own-code claim failed, 1, 3 and 6 of 16 cells; P1 as registered also mismatched a one-key prediction with an all-keys measurement) and G4b (sealed, INDETERMINATE: one-key isotropic perturbation in the discarded subspace reaches the second-order regime, the measured loss matches the quadratic prediction at the median, ratio 1.02 to 1.05 flat in eps up to 0.1, but 64 draws per cell give a 20 percent per-cell scatter that misses the per-triple bar, shown by a seed check to be draw noise; inside that regime the regret and bound statements are theorems given the operator, so their bars carry noise and nothing else) | paper Thm 7, prediction (iii) | G4 | `GET.Incompatibility.regret_nonneg`, `total_regret_ge`, `total_regret_attained`, `total_regret_zero_iff`, `deficiency_pos_iff`, with Ky Fan's maximum principle taken as the named hypotheses `KyFanBound` and `KyFanAttained` (not in Mathlib); the identification of attaining projectors with top-k eigenspaces is inside those hypotheses |
| GET-10 | Menu-dependent admissibility violates the weak axiom | `[proved]` / `[predicted]` (in evaluators) | paper Thm 8, prediction (v) | G6 | none |
| GET-11 | Threshold tracks budget | `[demonstrated]` on a computational evaluator (G3, sealed, PASS: a language-model scorer's indifference threshold is 0.87 to 0.88 of the rendering step at 0 to 3 decimals and of the resolution a report of 2 to 6 tokens affords, identical at bfloat16, 8-bit and 4-bit weights, and every pair separated by 20 is ordered alike in every cell) / `[predicted]` for human evaluators (G7) | paper prediction (i), Thm 2(b) | G3, G7 | none; the measurement is `experiments/G3/results.json` under sealed `PREREG-G3.md` |
| GET-12 | Hull law holds in a population with a known consequence map | `[demonstrated]` as a population tendency (G2, ANES 1972: 50 percent of 489 testable respondents violate at least once against a shuffle null of 66 percent, permutation p below 0.005, five replication pairs agree); NOT demonstrated as the deterministic statement, which half the respondents fail  Second world (G9, sealed, INDETERMINATE overall): on NFL play calls 2015 to 2024, 962 evaluator-by-cell units, the primary plane passes at 8 of 402 testable units violating, 0.0199 against a shuffle null of 0.2286, ten null standard deviations below and permutation p zero, at both the sealed floor and the sensitivity floor; of six planes carrying bars four pass, one is indeterminate and one refutes with violations MORE common than chance; the class is NOT raised to `[replicated]`, because a gate carrying a refuting plane and an INDETERMINATE verdict is not a replication | paper prediction (iv) | G2, G9 | `GET.HullLaw.hull_law` (the theorem); the measurement is `experiments/G2/results.json` under sealed `PREREG-G2.md` |
| GET-13 | Ethics-stack correspondence: deontic gate is K, invariance principle is factoring through the quotient | `[posited]` | paper Sec 7 | none | none |
| GET-14 | Closed-form combinatorial characterization of representability | `[open]` | paper Sec 3.4 | none | none |
| GET-15 | Loss aversion (asymmetric metric) and lexicographic priority as metric | not claimed | paper remark after Prop 2 | none | none |
| GET-9w | Witness. At ranks 2 to 8 a shared code applied to all 1,024 keys at once gives a measured attention loss 20 to 330 times the second-order prediction; reduced to two causes, the registered prediction was a one-key operator while the measurement coded every key, and the codes sat far outside the quadratic regime (mean KL about 4 nats per query); absorbed by the measurement definition | `[witness]` | G4 record in `CAMPAIGN.md`, `experiments/G4/results.json` | G4 | none |
| GET-9r | Revised. In response to GET-9w the shared-code test was re-registered as `experiments/G4b/PREREG-G4B.md` (blob d6d1c44f), measuring one key at each probed operating point perturbed by a small isotropic error in the discarded subspace, the object the formula predicts, along a perturbation ladder; what changed is the measurement, not the theorem, the operators, the cells, or the codes | `[revised]` | G4b record in `CAMPAIGN.md` | G4b | none |
| GET-16 | Observability and identifiability at a budget: the read operator of a linear consumer is positive semidefinite for a positive semidefinite output metric; a perturbation in its kernel is distinguishable at no budget; for a positive definite output metric distinguishability at budget zero is injectivity of the consumer (classical observability); a perturbation of size rho along v is distinguishable at budget B iff v^T P v > B^2 / rho^2; distinguishability is antitone in the budget; d_obs(B, rho) is antitone in B, equals the positive-eigenvalue count at B = 0, and is zero once every lambda_i rho^2 <= B^2; single-direction probes learn the identifiable coordinates and diagonal brackets and nothing off the diagonal; probes at one radius identify the operator exactly up to the pencil s P + (1 - s)(B^2/rho^2) I, whose analytic centre is the pencil's end, and two radii identify it | `[proved]` (article 0.2, 2026-09-09, Theorems 1 and 2 machine-checked; Proposition 2 and Corollaries 3 and 4 proved in the article, not machine-checked) / `[demonstrated]` in finite samples for the single-direction reading (OD gate D1, sealed: verdicts and brackets exact in 300 of 300 evaluators) and for the pencil's end (40 of 40 evaluators); `[exploratory]` for the recovery factor, D1 INDETERMINATE by its grader, five of six well-crossed cells within 0.30 of chance and the sixth at 0.303 on the above-threshold eigenvalues | `articles/2026-09-09-identifiability-at-a-budget.md` | OD D0, D1 | `GET.readOperator_posSemidef`, `kernel_not_distinguishable`, `distinguishable_zero_iff`, `distinguishable_scaled_iff`, `distinguishable_antitone`, `dObs_antitone`, `dObs_zero`, `dObs_eq_zero_of_large_budget` in `lean/GET/Identifiability.lean`; the Gramian integral is taken as the consumer's matrix, not derived |

Theorem numbers refer to the paper at draft 0.2 and are renumbered here when the paper renumbers.
| GET-16r | Proposition 1 (observational Lyapunov exponent) restated with the window bound log(lambda_max / lambda_min) / (2 (t - t0)) for positive definite observers, the condition for the smaller case, and "undefined" in place of "can be zero" for a perturbation confined to an invariant kernel, whose observational length is zero and to which Definition 3 does not apply | `[revised]` (article 0.3, 2026-09-09, made when gate D3 was registered and before its seal) / `[demonstrated]` in finite samples for the bound (3,200 of 3,200 windows), the smaller case (64 of 64 starts), the control and the kernel-start transient; the horizon's observer dependence `[exploratory]`, real in direction at every budget and short of the registered level at the largest (OD D3, INDETERMINATE by its grader) | `articles/2026-09-09-identifiability-at-a-budget.md` Section 6 | OD D3 | `GET.window_bound_scalar`, `GET.window_bound`, `GET.window_exponent_bound`, `GET.horizon_subset_of_le`, `GET.horizon_subset_of_budget` in `lean/GET/Horizon.lean` (standard axioms); the window bound is the exact bar E1 of D3 |
| GET-17 | The horizon offset law: an observer's predictability horizon differs from the full reader's by the first-passage time of its log read-fraction process along the leading Lyapunov direction, T_O(B) - T_full(B) = inf{t : L + g_O >= log B} - inf{t : L >= log B}, computable on one long trajectory; zero for the full reader, equal across a symmetry, inside the positive definite bracket; the mean-log-fraction form is not the law | `[proved]` as an identity (article 0.4, Proposition 3) / `[demonstrated]` in finite samples (OD gate D3v2, sealed, PASS: 22 of 22 cells within 0.26 time units on Lorenz-63 and Lorenz-96, order exact, the constant-fraction form refuted by the gate's probe) | `articles/2026-09-09-identifiability-at-a-budget.md` Section 6 | OD D3v2 | none; the identity is one line and the measurement is `observation-theory-campaigns/experiments/OD/D3v2/results.json` under sealed `PREREG-D3V2.md` |
| GET-12w | Witness. On NFL play calls the hull law is violated MORE often than chance on the expected points added against first down probability plane, 0.3000 of 450 testable units against a null of 0.2211, permutation p 1.0, while four sibling planes on the same units and the same code pass. Not reduced. The offered cause, that the two coordinates are near redundant so the menu degenerates toward a line where Theorem 3 makes single-peakedness the requirement, was tested against the other nine planes and fails, rank correlation between collinearity and violation rate -0.09. Absorbed by nothing yet | `[witness]` | G9 record in `CAMPAIGN.md`, `experiments/G9/RESULTS-G9.md` | G9 | none |
| GET-11w | Witness. The budget prediction could not be measured on NFL play calls because the identification estimator recovers no metric from the league's pooled choices, the max-margin being zero and the recovered metric of order 1e-9 in both pressure strata. Reduced by a pool ladder: single game states are representable in 25 of 25 fits, the margin falls from 2.36 at one state to 0.0766 at eight, and the program collapses at twelve. One evaluator serves about eight game states and a stratum holds 189. Absorbed by the arm's design assumption that one metric and ideal hold still across a stratum, not by the prediction, which is untested | `[witness]` | G9 budget record in `CAMPAIGN.md`, `experiments/G9/RESULTS-G9-budget.md` | G9 | none |
| GET-12r | Revised. GET-12w recorded the hull law violated above chance on one G9 plane as a live counterexample with its cause open. G9b (sealed, PASS on both claims) reads the same coordinates with thirteen play calls at two, three and four coordinates and finds them below the shuffle null at every one, 0 of 26 subsets above null. The characterization file shows G9's four actions are testable only on a plane and that one standard error of sampling noise moves its refuting plane by 0.089 against a margin of 0.079. What changed is the status of the witness, from a counterexample to the law to a witness absorbed by the measurement, as GET-9w was. The law is unchanged and GET-12 is not raised, since G9 as a gate remains INDETERMINATE | `[revised]` | G9b record in `CAMPAIGN.md`, `experiments/G9b/RESULTS-G9B.md`, `experiments/G9b/ACTION-SPACE-CHARACTERIZATION.md` | G9b | none |
| GET-18 | Bridge, not a theorem. If deliberation time buys independent reads of a position at a constant rate, the result that the mean of n sampled scores lowers a judge's threshold as n to the minus one half carries over to seconds, and a human's indifference threshold falls as the inverse square root of the time budget. With the evaluator held fixed it predicts threshold ratios between budgets with no free parameter. It failing would leave GET-11 standing | `[refuted]` in within-player chess across clocks (G7a-r, sealed, C2r FAIL: exponent -0.041, interval -0.113 to +0.035, on August 2026; the same players are 0.87 times as coarse at one minute as at five where the bridge says 2.24). G7a under its own reader was INDETERMINATE in both months. GET-11 for human evaluators is untouched and stays `[predicted]`: the fall was neither shown nor excluded below about -0.11 | `experiments/G7a/PREREG-G7A.md` Section 1, claim C2; `experiments/G7a/PREREG-G7AR.md` claim C2r | G7a, G7a-r, `experiments/G7a/RESULTS-G7A.md` | none |
